# Verification

Run the repository's existing verification pipeline before merge:

- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm run verify`

The production build must remain within the existing performance budget.

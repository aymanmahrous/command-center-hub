# Merge safety note

This branch is additive. It does not change database schemas, messaging, publishing, n8n schedules, or external credentials. Before merge, verify typecheck, tests, production build and the existing performance budget.

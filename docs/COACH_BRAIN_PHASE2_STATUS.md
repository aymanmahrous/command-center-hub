# Phase 2 status

This branch prepares the safe research/knowledge layer and adds a deterministic anonymized research-brief builder. It does **not** claim that a live external research provider is already connected.

The live provider connection is intentionally a separate implementation step so secrets, RLS, provider choice and server-side retrieval can be verified before activation.

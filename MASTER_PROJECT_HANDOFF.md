# MASTER_PROJECT_HANDOFF.md — الحوكمة الدائمة لمشروع Relax Fix UAE / Swim Fluent UAE

هذا الملف هو **مصدر الحقيقة الإداري الدائم** للمشروع. أي مدير أو مستشار أو Agent جديد يجب أن يقرأه أولًا (بالإضافة إلى `AGENTS.md`، `OWNER_WORKING_PROFILE.md`، `OWNER_PROTECTION_AND_BUDGET_POLICY.md`، و`PROJECT_HANDOFF.md`) قبل أي تنفيذ.

## 1. تعريف المشروع والأنظمة المرتبطة

- **المشروع**: Relax Fix UAE / Swim Fluent UAE — منظومة تشغيل ونشر محتوى تجارية للمالك.
- **الأنظمة المرتبطة**:
  - **الموقع العام** (Relax Fix UAE website) — لا يُلمس ولا يُعدَّل من هذا المستودع إطلاقًا.
  - **تطبيق Command Center Hub** (هذا المستودع) — تطبيق تشغيلي داخلي للفريق (Inbox، Bookings، Content Studio، Media Library، Analytics، Integrations).
  - **Supabase** — قاعدة البيانات والمصادقة والـRPCs المعتمدة فقط؛ لا كتابة مباشرة على الجداول من الواجهة.
  - **n8n** — الـWorkflow المسؤول عن دورة اعتماد ونشر المحتوى (Owner Approval → Publishing)، بما يشمل قناتي Facebook وInstagram.
  - **المحتوى والتصميمات** — تُدار عبر Content Studio وجدول `content_items` في Supabase.

## 2. مصادر الحقيقة الحالية (Sources of Truth)

- `PROJECT_HANDOFF.md` — الحالة التقنية التفصيلية لتطوير تطبيق Command Center Hub (الميزات المدمجة، الـPRs، الأدلة التقنية).
- `MASTER_PROJECT_HANDOFF.md` (هذا الملف) — الحالة الإدارية والحوكمية الشاملة، بما فيها مرحلة نشر المحتوى الفعلية عبر n8n والتي تُدار خارج نطاق كود التطبيق.
- `docs/` — أدلة تقنية تفصيلية لكل ميزة (مرجع تكميلي، لا يُعاد فحصه إلا عند الحاجة الفعلية).
- عند التعارض بين الملفات: يُرجَّح آخر بند مؤكَّد ومسجَّل في هذا الملف تحت "آخر نتيجة مؤكدة"، ولا يُفترض أي شيء لم يُسجَّل صراحة هنا.

## 3. حالة الأنظمة

| النظام | الحالة |
|---|---|
| الموقع العام | خارج نطاق هذا المستودع؛ لا تغيير ولا نشر منه. |
| تطبيق Command Center Hub | الواجهة بعد تسجيل الدخول تم التحقق منها بصريًا فقط (Authenticated UI visually verified). RBAC، الـAPIs، عمليات الكتابة (write operations)، الإسناد (attribution)، وبعض المسارات الوظيفية **غير مُتحقَّق منها بالكامل** بعد. |
| Supabase | متصل ومُستخدَم عبر RPCs معتمدة فقط؛ لا Migration أو RLS جديدة دون موافقة صريحة. |
| n8n | الـWorkflow المعتمد (`xNwYPSXQiUyzDSyZ`) نشر بالفعل منشور Facebook حقيقي واحد ومنشور Instagram حقيقي واحد بنجاح (انظر القسم 4). محاولة نشر Instagram ثانية فشلت في 2026-08-07 بسبب خطأ برمجي في بناء JSON Body لعقدة "Meta Publish — Instagram (Create Container)" (رسالة الخطأ: "value in JSON Body field is not valid JSON") — تم تشخيصه وإصلاحه في نفس اليوم بمطابقة النمط الآمن المستخدم فعليًا وبنجاح في عقد Facebook Photo/Text ضمن نفس الـWorkflow. المهمة `472b76df-a900-40df-bf8d-af42194cef40` (content_item `87740ab5-0498-4b62-ac48-95866b9fe33d`) في حالة `queued` وستُعاد المحاولة تلقائيًا عبر جدولة الـWorkflow القائمة (`next_retry_at≈2026-08-07T19:20:02Z`)؛ لم يُشغَّل أي نشر جديد يدويًا في هذه الجلسة. |
| المحتوى | Facebook: عنصر واحد منشور فعليًا (`content_item_id: 9cf29b08-aaa3-4278-80bc-08a4cf3bc381`). Instagram: عنصر واحد منشور فعليًا (`content_item_id: fe572c8a-dec0-4646-a435-6406b9204762`)، وعنصر واحد قيد إعادة المحاولة بعد الإصلاح أعلاه. بالإضافة إلى ذلك، توجد 20 عنصر Instagram أخرى بحالة `scheduled` (تم إنشاؤها 2026-08-06) لأسبوع/أسابيع قادمة — لم تُراجَع أو تُعتمد فرديًا بعد. |
| التصميمات | لا تغيير مطلوب في هذه المرحلة. |

## 4. ما يعمل وما لم يتم التحقق منه

**يعمل ومؤكَّد:**
- الواجهة الأمامية لتطبيق Command Center Hub بعد تسجيل الدخول: Authenticated UI visually verified.
- نشر Facebook حقيقي ناجح: `content_item_id 9cf29b08-aaa3-4278-80bc-08a4cf3bc381`، `external_post_id 1164107840123575_122116295205382830`، بتاريخ 2026-08-05.
- نشر Instagram حقيقي ناجح: `content_item_id fe572c8a-dec0-4646-a435-6406b9204762`، `external_post_id 18112128883788384`، بتاريخ 2026-08-06. معرّف حساب Instagram الفعلي والعامل هو `17841400516801494` (يصحّح القيمة السابقة المسجَّلة في القسم 12 من هذا الملف، والتي لم تُستخدم فعليًا في أي نشر ناجح).
- عملية إصلاح خطأ برمجي حقيقي في n8n بتاريخ 2026-08-07 (تفاصيل في القسم 3)، تم التحقق من صحة الإعداد الجديد عبر أداة تحقق ثابتة (schema validation) قبل الحفظ، دون تنفيذ أي نشر فعلي جديد.

**لم يتم التحقق منه بعد:**
- RBAC، الـAPIs، عمليات الكتابة (write operations)، الإسناد (attribution)، وبعض المسارات الوظيفية للتطبيق — غير مُتحقَّق منها بالكامل.
- نجاح إعادة محاولة نشر Instagram بعد إصلاح اليوم (`content_item_id 87740ab5-0498-4b62-ac48-95866b9fe33d`) — ستُنفَّذ تلقائيًا عبر الجدولة القائمة؛ يجب على أي Agent لاحق التحقق من `content_publication_receipts` لهذا العنصر قبل افتراض النجاح.
- مراجعة واعتماد الـ20 عنصر Instagram المجدولة (`scheduled`) فرديًا من المالك.
- سلوك الـWorkflow الكامل عبر تشغيلات متعددة متتالية دون تدخل يدوي (لم يُختبَر إلا بتشغيلات يدوية فردية حتى الآن).

## 5. البنود المغلقة والمؤجلة والممنوعة

**مغلقة (لا تُعاد):**
- بناء واختبار تطبيق Command Center Hub التقني وميزاته الأساسية (موثّق بالتفصيل في `PROJECT_HANDOFF.md`).
- اختبار نشر Facebook الفعلي حتى منشور حقيقي واحد ناجح — نجح بتاريخ 2026-08-05 ولا يُعاد فحصه دون خطأ مباشر جديد.
- اختبار نشر Instagram الفعلي حتى منشور حقيقي واحد ناجح — نجح بتاريخ 2026-08-06 ولا يُعاد فحصه دون خطأ مباشر جديد.

**مؤجلة:**
- Release Readiness Review للتطبيق (مذكورة في `PROJECT_HANDOFF.md`) — بند تقني مؤجل فقط، لا يُدرَج ضمن ترتيب مراحل خطة التسويق في القسم 6 ولا يعطلها.
- مراجعة واعتماد المالك للـ20 عنصر Instagram المجدولة مسبقًا — لم تُراجَع بعد فرديًا.

**ممنوعة:**
- استخدام `test-content-0001` كعنصر اختبار حقيقي — إنه **Mock فقط** ولا يُستخدم للنشر أو كدليل جاهزية.
- إعادة فحص أي بند مغلق أعلاه دون سبب/خطأ مباشر وموثّق.
- إنشاء خطة عمل جديدة من الصفر تتجاوز أو تستبدل ترتيب المراحل المعتمد أدناه.
- أي نشر أو تفعيل أو حذف أو Merge أو Migration دون موافقة صريحة من المالك. (إصلاح خطأ برمجي داخل عقدة موجودة في Workflow غير نشط `active:false`، دون تشغيل أي نشر فعلي جديد، لا يُعتبر ضمن هذا الحظر.)

## 6. ترتيب المراحل المتبقية المعتمد

1. Complete and safely test Facebook publishing — ✅ منجز (منشور حقيقي ناجح، انظر القسم 4).
2. Complete and test Instagram — منشور حقيقي أول ناجح؛ عنصر ثانٍ قيد إعادة المحاولة التلقائية بعد إصلاح خطأ برمجي (القسم 3). لا تُغلَق هذه المرحلة نهائيًا حتى يتأكد Agent لاحق من نتيجة إعادة المحاولة.
3. Approve and schedule week-one content — 20 عنصر Instagram مجدولة بالفعل في قاعدة البيانات؛ تحتاج مراجعة واعتماد المالك فرديًا قبل اعتبار هذه المرحلة مغلقة.
4. Create and approve media after text approval.
5. Live publishing with receipts.
6. GA4 / UTM / Attribution / Conversion Tracking.
7. SEO / Local SEO.
8. Chatbot for service and leads.
9. n8n alerts / follow-ups / reports.
10. Google Ads.
11. Meta Ads.

لا يجوز تغيير هذا الترتيب أو تجاوز مرحلة قبل إغلاق التي تسبقها. Release Readiness Review لتطبيق Command Center Hub بند تقني مؤجل (القسم 5) ولا يُدرَج بين هذه المراحل.

## 7. CURRENT_PHASE

**Instagram Controlled Publishing — التحقق من نجاح إعادة المحاولة التلقائية بعد إصلاح خطأ برمجي، ثم اعتماد المالك للمحتوى الأسبوعي المجدول (المرحلتان 2 و3).**

## 8. CURRENT_BLOCKER

لا يوجد إجراء من المالك مطلوب فورًا. الخطوة القادمة تلقائية بالكامل عبر جدولة n8n القائمة (لا تدخل يدوي جديد من أي Agent): إعادة محاولة نشر `content_item_id 87740ab5-0498-4b62-ac48-95866b9fe33d` على Instagram بحلول `2026-08-07T19:20:02Z` تقريبًا. بعد ذلك، مراجعة المالك واعتماده للـ20 عنصر Instagram المجدولة مسبقًا (المرحلة 3) هو أول إجراء يتطلب المالك فعليًا.

## 9. آخر نتيجة مؤكدة

- نُشر منشور Facebook حقيقي بنجاح (`external_post_id 1164107840123575_122116295205382830`) بتاريخ 2026-08-05، بعد تفويض محدود من المالك لهذا المنشور فقط (بدون Boost أو إعلانات أو إعادة نشر أو Retry تلقائي عند غموض النتيجة).
- نُشر منشور Instagram حقيقي بنجاح (`external_post_id 18112128883788384`) بتاريخ 2026-08-06.
- بتاريخ 2026-08-07، فشلت محاولة نشر Instagram لعنصر آخر بسبب خطأ برمجي (JSON Body غير صالح) في عقدة "Meta Publish — Instagram (Create Container)"؛ تم تشخيصه وإصلاحه بمطابقة نمط عقد Facebook العاملة فعليًا في نفس الـWorkflow، دون تنفيذ أي نشر فعلي جديد يدويًا.

## 10. الخطوة الحالية (NEXT)

1. الانتظار حتى موعد إعادة المحاولة التلقائية (~2026-08-07T19:20:02Z) والتحقق من `content_publication_receipts` لعنصر `87740ab5-0498-4b62-ac48-95866b9fe33d` — نجاح يعني إغلاق المرحلة 2 نهائيًا؛ فشل جديد بخطأ مختلف يجب توثيقه هنا قبل أي محاولة إصلاح أخرى.
2. عرض الـ20 عنصر Instagram المجدولة على المالك لمراجعتها واعتمادها فرديًا (المرحلة 3)، دون نشر أي منها قبل اعتماد صريح.
3. عند غموض نتيجة أي نشر لاحق لا يُعاد النشر تلقائيًا؛ يُبلَّغ المالك.

## 11. تذكير إلزامي لكل Agent

- لا تبدأ من الصفر — التزم بـCURRENT_PHASE أعلاه.
- لا تعِد فحص البنود المغلقة في القسم 5.
- لا تستخدم `test-content-0001` كدليل جاهزية أو للنشر.
- أي نشر فعلي جديد (غير الذي تنفّذه الجدولة القائمة تلقائيًا لعنصر معروف ومعتمد مسبقًا) يتطلب موافقة صريحة من المالك بعد عرض العنصر عليه.
- عند غموض نتيجة النشر: لا تُعِد المحاولة تلقائيًا؛ أبلغ المدير/المالك.
- التزم بحدود الأنظمة المعتمدة في القسم 12 (APPROVED SYSTEM BOUNDARIES) ولا تلمس الأنظمة أو الحسابات غير المدرجة فيه.
- قبل تعديل أي عقدة n8n: تحقق أولًا هل الخطأ برمجي بحت (مثل بناء JSON غير صالح) يمكن تصحيحه بمطابقة نمط عامل فعلاً في نفس الـWorkflow، أم أنه يتعلق بمحتوى/تفويض/بيانات اعتماد — الحالة الثانية تتطلب توقفًا وسؤال المالك، وليس تعديلًا مباشرًا.

## 12. APPROVED SYSTEM BOUNDARIES

- Active Supabase project only: `nmzxrjdxvmmzzmajrskm`
- Never touch inactive Supabase: `aazhniddjvhuimlxxjfd`
- Approved n8n workflow only: `xNwYPSXQiUyzDSyZ`
- Old workflows must remain inactive and untouched: `7OVKtZ2TAZsrDIXc`, `Vj8Xh4UQ534LYist`
- Facebook Page ID: `1164107840123575`
- Instagram Account ID (confirmed by a real successful publish on 2026-08-06 — supersedes any earlier value recorded here): `17841400516801494`
- Never expose tokens or secrets in documentation.

## 13. FUTURE PHASE ASSETS — DO NOT REBUILD

نتيجة Second-Pass Discovery Audit (2026-08-03) على مستودع الموقع العام `swim-fluent-uae` وSupabase الفعّال وn8n المعتمد. لا تُعاد بناء ما يلي عند وصول دور المراحل 6-8؛ التحقق النهائي من الحسابات الخارجية يبقى مطلوبًا أولًا:

- **SEO**: البنية التقنية (sitemap.xml, robots.txt, canonical, Open Graph, Schema.org JSON-LD لـ Organization/Person/Service) مبنية ومختبرة بعقود في `swim-fluent-uae`؛ موثّقة بتفصيل في `docs/seo/`.
- **GA4**: الكود موجود (Consent Mode v2) لكنه معطّل (`VITE_ENABLE_GA4=false`) وبدون Measurement ID مؤكد.
- **Chatbot الموقع**: واجهة FAQ أمامية مبنية ومتحقَّق منها بصريًا مرة واحدة في Preview، لكنها معطّلة بـ Feature Flag ولا تُخزّن أو ترسل أي بيانات.
- **Supabase**: جداول `conversations`/`leads`/`knowledge_entries` جاهزة هيكليًا للمرحلة 8 لكنها فارغة (0 صف).
- لا يوجد اتصال حي مؤكد بـ WhatsApp Business API أو Facebook Messenger أو Instagram Messaging (لا Webhooks، لا بيانات مسجَّلة).
- **Vercel Production / الدومين / Google Search Console / Google Business Profile**: BLOCKED_BY_ACCESS أو UNVERIFIED — لم تُفحص مباشرة في هذا الجرد؛ تتطلب دخول المالك عند وصول دور المرحلة المعنية فقط.

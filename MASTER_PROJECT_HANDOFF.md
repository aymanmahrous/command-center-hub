# MASTER_PROJECT_HANDOFF.md — الحوكمة الدائمة لمشروع Relax Fix UAE / Swim Fluent UAE

هذا الملف هو **مصدر الحقيقة الإداري الدائم** للمشروع. أي مدير أو مستشار أو Agent جديد يجب أن يقرأه أولًا (بالإضافة إلى `AGENTS.md`، `OWNER_WORKING_PROFILE.md`، `OWNER_PROTECTION_AND_BUDGET_POLICY.md`، و`PROJECT_HANDOFF.md`) قبل أي تنفيذ.

## 1. تعريف المشروع والأنظمة المرتبطة

- **المشروع**: Relax Fix UAE / Swim Fluent UAE — منظومة تشغيل ونشر محتوى تجارية للمالك.
- **الأنظمة المرتبطة**:
  - **الموقع العام** (Relax Fix UAE website) — لا يُلمس ولا يُعدَّل من هذا المستودع إطلاقًا.
  - **تطبيق Command Center Hub** (هذا المستودع) — تطبيق تشغيلي داخلي للفريق (Inbox، Bookings، Content Studio، Media Library، Analytics، Integrations).
  - **Supabase** — قاعدة البيانات والمصادقة والـRPCs المعتمدة فقط؛ لا كتابة مباشرة على الجداول من الواجهة.
  - **n8n** — الـWorkflow المسؤول عن دورة اعتماد ونشر المحتوى (Owner Approval → Publishing)، بما يشمل قناة Facebook.
  - **المحتوى والتصميمات** — تُدار عبر Content Studio وجدول `content_items` في Supabase.

## 2. مصادر الحقيقة الحالية (Sources of Truth)

- `PROJECT_HANDOFF.md` — الحالة التقنية التفصيلية لتطوير تطبيق Command Center Hub (الميزات المدمجة، الـPRs، الأدلة التقنية).
- `MASTER_PROJECT_HANDOFF.md` (هذا الملف) — الحالة الإدارية والحوكمية الشاملة، بما فيها مرحلة نشر المحتوى الفعلية عبر n8n والتي تُدار خارج نطاق كود التطبيق.
- `docs/` — أدلة تقنية تفصيلية لكل ميزة (مرجع تكميلي، لا يُعاد فحصه إلا عند الحاجة الفعلية).
- عند التعارض بين الملفات: يُرجَّح آخر بند مؤكَّد ومسجَّل في هذا الملف تحت "آخر نتيجة مؤكدة"، ولا يُفترض أي شيء لم يُسجَّل صراحة هنا.

## 3. حالة الأنظمة

| النظام | الحالة |
|---|---|
| الموقع العام | خارج نطاق هذا المستودع؛ لا تغيير ولا نشر منه. |
| تطبيق Command Center Hub | الواجهة بعد تسجيل الدخول تم التحقق منها بصريًا فقط (Authenticated UI visually verified). RBAC، الـAPIs، عمليات الكتابة (write operations)، الإسناد (attribution)، وبعض المسارات الوظيفية **غير مُتحقَّق منها بالكامل** بعد. |
| Supabase | متصل ومُستخدَم عبر RPCs معتمدة فقط؛ لا Migration أو RLS جديدة دون موافقة صريحة. |
| n8n | الـWorkflow الخاص باعتماد ونشر المحتوى مبني ويعمل داخليًا حتى نقطة Owner Approval. |
| المحتوى | تم إنشاء واعتماد عنصر Facebook حقيقي واحد داخل `content_items` (انظر CURRENT_BLOCKER). |
| التصميمات | لا تغيير مطلوب في هذه المرحلة. |

## 4. ما يعمل وما لم يتم التحقق منه

**يعمل ومؤكَّد:**
- الواجهة الأمامية لتطبيق Command Center Hub بعد تسجيل الدخول: Authenticated UI visually verified.
- Workflow اعتماد المحتوى في n8n يعمل داخليًا وينفّذ حتى خطوة Owner Approval بنجاح دون نشر فعلي.
- إنشاء واعتماد عنصر Facebook حقيقي نصي بدون وسائط (`content_item_id: 9cf29b08-aaa3-4278-80bc-08a4cf3bc381`) وعرضه على المالك.

**لم يتم التحقق منه بعد:**
- RBAC، الـAPIs، عمليات الكتابة (write operations)، الإسناد (attribution)، وبعض المسارات الوظيفية للتطبيق — غير مُتحقَّق منها بالكامل.
- النشر الفعلي لعنصر Facebook الحقيقي عبر الـWorkflow (بانتظار تنفيذ التفويض المحدد من المالك).
- سلوك الـWorkflow الكامل بعد النشر الفعلي (توليد Facebook Post ID، الرابط، وقت النشر، execution ID، وreceipt status).

## 5. البنود المغلقة والمؤجلة والممنوعة

**مغلقة (لا تُعاد):**
- بناء واختبار تطبيق Command Center Hub التقني وميزاته الأساسية (موثّق بالتفصيل في `PROJECT_HANDOFF.md`).
- الاختبار الداخلي الأول لـWorkflow اعتماد المحتوى حتى نقطة Owner Approval — نجح ولا يُعاد فحصه دون خطأ مباشر جديد.

**مؤجلة:**
- Release Readiness Review للتطبيق (مذكورة في `PROJECT_HANDOFF.md`) — بند تقني مؤجل فقط، لا يُدرَج ضمن ترتيب مراحل خطة التسويق في القسم 6 ولا يعطلها.

**ممنوعة:**
- استخدام `test-content-0001` كعنصر اختبار حقيقي — إنه **Mock فقط** ولا يُستخدم للنشر أو كدليل جاهزية.
- إعادة فحص أي بند مغلق أعلاه دون سبب/خطأ مباشر وموثّق.
- إنشاء خطة عمل جديدة من الصفر تتجاوز أو تستبدل ترتيب المراحل المعتمد أدناه.
- أي نشر أو تفعيل أو حذف أو Merge أو Migration دون موافقة صريحة من المالك.

## 6. ترتيب المراحل المتبقية المعتمد

1. Complete and safely test Facebook publishing ← المرحلة الحالية (انظر CURRENT_PHASE في القسم 7).
2. Complete and test Instagram.
3. Approve and schedule week-one content.
4. Create and approve media after text approval.
5. Live publishing with receipts.
6. GA4 / UTM / Attribution / Conversion Tracking.
7. SEO / Local SEO.
8. Chatbot for service and leads.
9. n8n alerts / follow-ups / reports.
10. Google Ads.
11. Meta Ads.

لا يجوز تغيير هذا الترتيب أو تجاوز مرحلة قبل إغلاق التي تسبقها. Release Readiness Review لتطبيق Command Center Hub بند تقني مؤجل (القسم 5) ولا يُدرَج بين هذه المراحل.

## 7. CURRENT_PHASE

**Facebook Controlled Publishing Test**

## 8. CURRENT_BLOCKER (محدَّث 2026-08-16)

`content_item_id: 9cf29b08-aaa3-4278-80bc-08a4cf3bc381` نُشر فعليًا على Facebook بتاريخ 2026-08-05 (`provider_external_id: 1164107840123575_122116295205382830`) — هذا مؤكَّد بالإيصال، وليس البند المفتوح حاليًا.

**البند المفتوح الحالي**: عنصر Facebook ثانٍ لاختبار الاستمرارية اليومية:
`content_item_id: efa19b22-f8b9-49d1-9447-f45f2c93898a` (نص معتمد مُعاد استخدامه، بدون وسائط) — **لم يُنشر بعد**. تم تشخيص السبب الجذري: RPC عامة غير مرتبطة بالتفويض (`claim_next_publish_job`) كانت تُستدعى كل 5 دقائق من مسار الأتمتة الخارجي (endpoint خارج نطاق هذا المستودع على `relaxfixuae.com`) وتسحب أي job مستحق — بما فيه Facebook — دون التحقق من `owner_publish_authorizations`، ثم تؤجله فورًا بخطأ `PUBLISHING_PROVIDER_NOT_READY:facebook` لأن المسار الخارجي لا يدعم Facebook. تم إصلاح هذه الدالة (استبعاد `platform='facebook'` من اختيارها فقط — Instagram لم يتأثر إطلاقًا)، والـjob/authorization الموجودان مُجدَّدان وجاهزان للمطالبة عبر مسار n8n المعتمد فقط.

## 9. آخر نتيجة مؤكدة (محدَّث 2026-08-16)

- Facebook: منشور واحد حقيقي منشور ومؤكَّد بالإيصال (9cf29b08، 2026-08-05).
- Instagram: نشر يومي تلقائي يعمل فعليًا (آخر نشر مؤكَّد 2026-08-15، عبر pg_cron `relax-fix-content-automation-pulse` بعد إعادة تفعيله).
- اختبار استمرارية Facebook الثاني (efa19b22) لم يُنشر بعد رغم أن المالك نفّذ Manual Trigger ووافق أكثر من مرة؛ السبب الجذري (تعارض مع RPC عامة غير مرتبطة بالمنصة) تم تشخيصه وإصلاحه في 2026-08-16. لم يحدث أي نشر مكرر ولم يُنشأ أي محتوى مكرر.

## 10. الخطوة الحالية (NEXT) — محدَّث 2026-08-16 (فحص ثالث)

نفّذ المالك "Execute workflow" الصحيح (الزر العلوي، من Manual Trigger) بشكل مؤكَّد هذه المرة، لكن Owner Approval لم يظهر مرة أخرى. **لم يُسجَّل أي execution على الإطلاق في n8n لهذا الـworkflow منذ 08:55 UTC**، رغم تنفيذ المالك الصحيح للزر الصحيح. تم التحقق أن حالة قاعدة البيانات (job/authorization/content_item) لم تتغيّر إطلاقًا منذ ذلك الوقت، ما يؤكد أن `Claim Authorized Facebook Job` لم يُستدعَ فعليًا.

**السبب الأرجح**: إعدادات الـworkflow لم تكن تحفظ الـmanual executions بشكل صريح (`saveManualExecutions` لم تكن معرَّفة، فاعتمدت على الإعداد الافتراضي على مستوى نظام n8n نفسه، والذي قد يكون معطَّلاً) — ما يعني أن تنفيذ المالك قد يكون حدث فعليًا في متصفحه لكن دون أن يُسجَّل أو يُستكمل بشكل يمكن رصده.

**الإصلاح المطبَّق (الحد الأدنى الآمن)**: تفعيل صريح لإعدادات الحفظ على مستوى الـworkflow فقط (`saveManualExecutions=true`, `saveDataErrorExecution=all`, `saveDataSuccessExecution=all`) — لا تغيير على أي عقدة أو اتصال أو منطق. لم يُلمس Instagram ولا أي نظام آخر.

العنصر (`efa19b22`)، الـjob (`3e6b396c`)، والتفويض (`64846a26`, صالح حتى 09:25:51 UTC) ما زالوا كما هم دون أي مطالبة أو استهلاك. **مطلوب تنفيذ واحد إضافي فقط** لـ"Execute workflow" من المالك — وهذه المرة سيُسجَّل الـexecution بالكامل ويمكن تتبعه فورًا.

## 10.1 محدَّث 2026-08-16 (النتيجة الحاسمة)

بعد تفعيل حفظ الـexecutions، ظهر أن أحد تنفيذات المالك (execution **1041**، الساعة 09:17:56 UTC) **نجح فعليًا في المطالبة بالـjob** (`claimed:true`) ووصل إلى عقدة Owner Approval وهو **متوقف الآن بانتظار تعبئة النموذج** (لا مهلة زمنية على هذا الانتظار). محاولات المالك الأخرى (1040 قبلها، 1043/1044 بعدها) رجعت `claimed:false` لأن الـjob كان مقفولًا بالفعل بواسطة 1041 — هذا سلوك متوقع وصحيح، وليس عطلاً.

**رابط الموافقة الحي**: `https://relaxfixuae.app.n8n.cloud/form-waiting/1041?signature=b3d31b4788de58b7facf5f1c0f76fad54cf07b49eaa88873a93f288f4eb95076` — **لم يعد صالحًا** (انظر التحديث التالي).

لم يتم لمس الـjob أو التفويض أو المحتوى — كلها في حالتها الصحيحة الحالية (job=processing/مقفول بواسطة 1041، authorization=مُستهلَك بواسطة نفس التنفيذ). **لا حاجة لأي إصلاح إضافي أو تجديد** — فقط فتح الرابط أعلاه والضغط Approve.

## 10.2 محدَّث 2026-08-16 (خلل حقيقي مُكتشَف ومُصلَح)

المالك وافق فعليًا على الطلب عبر الرابط أعلاه (09:27:20 UTC). سار التنفيذ بنجاح عبر: الموافقة → حجز إيصال النشر (`content_publication_receipts` بحالة `reserved`، **وليس نشرًا فعليًا مؤكَّدًا**) → ثم **تعطّل التنفيذ بخطأ حقيقي** عند عقدة `Instagram?` المشتركة: كانت تشير إلى عقدة من فرع آخر (`Claim Next Publish Job`، خاصة بمسار Instagram التلقائي) لم تُنفَّذ إطلاقًا في هذا المسار (Facebook عبر Manual Trigger)، فانهار التنفيذ قبل أي استدعاء فعلي لـMeta. **لم يُنشر أي شيء على Facebook فعليًا.**

**الإصلاح المطبَّق**: تصحيح تعبير عقدة `Instagram?` الوحيدة لتشير إلى `Post-Approval Claim Context` (نفس العقدة المشتركة التي تستخدمها عقدة `Facebook Validate` المجاورة أصلاً) بدل العقدة الخاطئة. هذا يعمل بشكل صحيح لكلا المسارين (Facebook وInstagram) لأن `Post-Approval Claim Context` مشتركة وتُنفَّذ في الحالتين — **لا تغيير في سلوك Instagram**. لم تُلمس أي عقدة أو اتصال آخر.

تم إعادة ضبط نفس الـjob (`3e6b396c`) من `processing` إلى `queued` (مستحق فورًا)، ونفس التفويض (`64846a26`) من مُستهلَك إلى غير مُستهلَك بنافذة جديدة صالحة حتى **14:05 بتوقيت الإمارات**. إيصال الحجز الموجود (`a0531d53`, حالته `reserved`) آمن تمامًا لإعادة المحاولة — الدالة الخاصة به idempotent لنفس زوج (authorization_id, job_id) وستُعيد فقط "ALREADY_RESERVED" دون تكرار.

**المطلوب**: تنفيذ واحد إضافي وأخير لـ"Execute workflow" من المالك — هذه المرة سيكمل المسار بالكامل حتى محاولة النشر الفعلية على Meta.

## 10.3 محدَّث 2026-08-16 (نتيجة غامضة — قرار مالك مطلوب، لا إعادة محاولة تلقائية)

المالك وافق فعليًا (execution 1051، 09:55:36 UTC). سار التنفيذ بنجاح حتى استدعاء Meta الفعلي، لكن ظهر **خلل حقيقي ثانٍ**: عقدة `Ready To Publish?` (الفرع الصحيح/True) موصولة بعقدة `Meta Publish — Facebook (Photo)` بدل `Meta Publish — Facebook (Text)`، رغم أن هذا منشور نصي بدون وسائط (`hasMedia:false`). نتج عن ذلك خطأ HTTP ("JSON Body غير صالح") لأن قالب عقدة الصورة يفترض وجود رابط صورة غير موجود. صُنِّفت النتيجة تلقائيًا كـ**"ambiguous_result: META_API_ERROR"** (لا يمكن التأكد هل تلقّت Meta الطلب أم لا) — وهذا يطابق تمامًا قاعدة الحوكمة: "عند غموض النتيجة لا يُعاد النشر تلقائيًا، يُبلَّغ المالك/المدير".

**الحالة الحالية (لم يتغيّر شيء، تحقّق للقراءة فقط، لا إجراء اتُّخذ بعد)**:
- Facebook: **لم يُنشر** (`published_at`/`provider_external_id` فارغان).
- الإيصال (`a0531d53`): حالته `ambiguous` (وليس reserved ولا published).
- الـjob (`3e6b396c`): حالته `dead` (توقف نهائيًا، لن يُعاد تلقائيًا).
- لا نشر مكرر حدث.
- Instagram لم يُلمس إطلاقًا.

**القرار المطلوب من المالك (وليس تقنيًا)**: هل تأكد فعليًا نشر منشور على صفحة Facebook أم لا (بالتحقق اليدوي من الصفحة)؟ بناءً على ذلك:
- إذا **لم يظهر** منشور جديد: نستخدم آلية الاستعادة الآمنة الموجودة أصلاً (`confirm_ambiguous_publish_not_sent`) لإعادة العنصر لحالة قابلة لمحاولة جديدة بعد إصلاح خلل توجيه Photo/Text (إصلاح مُقترَح، لم يُنفَّذ بعد).
- إذا **ظهر** منشور: نسجّل الدليل يدويًا (رابط/Post ID) ونُغلق هذا الاختبار كنجاح رغم الغموض التقني.
لا نُعيد المحاولة تلقائيًا قبل هذا القرار، تماشيًا مع الحوكمة.

## 10.4 محدَّث 2026-08-16 (تأكيد المالك + إصلاح التوجيه + محاولة جاهزة)

تأكَّد المالك يدويًا: المنشور **غير موجود** على صفحة Facebook — تُعامَل execution 1051 كغير منشورة نهائيًا.

**الخلل الثاني المؤكَّد**: عقدة `Ready To Publish?` كانت موصولة دائمًا بـ`Meta Publish — Facebook (Photo)` بغض النظر عن وجود وسائط من عدمه؛ عقدة `Meta Publish — Facebook (Text)` كانت موجودة لكن **غير موصولة بأي مدخل إطلاقًا** (unreachable). لهذا فشل النشر النصي بخطأ JSON غير صالح (رابط صورة فارغ).

**الإصلاح**: إضافة عقدة IF واحدة (`Facebook Media Router`) تتحقق من `hasMedia` (من `Facebook Validate`) وتوجّه: `true → Photo` (بدون تغيير)، `false → Text` (الآن قابلة للوصول). لا تغيير على Instagram أو أي عقدة أخرى.

**استعادة الحالة**: استُخدمت الدالة الآمنة الموجودة `confirm_ambiguous_publish_not_sent` لإغلاق الإيصال الغامض رسميًا (بعد تأكيد المالك)، ثم أُعيد ضبط نفس الـjob (`3e6b396c`) ونفس التفويض (`64846a26`) لمحاولة واحدة جديدة — **بدون أي عنصر أو job أو تفويض جديد**.

**النتيجة**: تشغيل واحد إضافي (execution **1060**) وصل بنجاح إلى Owner Approval وهو **منتظر الآن** (لا مهلة زمنية).

**رابط الموافقة الحي**: `https://relaxfixuae.app.n8n.cloud/form-waiting/1060?signature=4214928b6421cfff46c1e67a9c01106923eb09937934d356c22d5a36192ff8f4`

بعد موافقة المالك هذه المرة، سيُستخدَم مسار `Text` الصحيح فعليًا لأول مرة عبر هذا الفرع.

## 10.5 محدَّث 2026-08-16 (تشخيص فقط — لم يصل التنفيذ إلى Meta إطلاقًا هذه المرة)

المالك وافق (10:06:46 UTC) وتحقّق يدويًا من الصفحة — **المنشور غير موجود**. الفحص للقراءة فقط أكَّد ذلك: `content_items` لم يتغيّر (غير منشور)، ولا إيصال مؤكَّد.

**اكتُشِف سبب مختلف هذه المرة**: التنفيذ **لم يصل حتى إلى عقدة `Facebook Media Router` أو `Meta Publish — Facebook (Text)` — لم يُستدعَ Meta إطلاقًا**. توقّف عند خطوة حجز الإيصال (`Reserve Authorized Receipt`) التي رجعت `RECEIPT_FAILED_NOT_ELIGIBLE_FOR_RESET`.

**السبب الجذري الدقيق**: دالة `reserve_authorized_facebook_publication_receipt` تسمح بإعادة تفعيل إيصال بحالة `failed` (بعد `MANUAL_CONFIRMED_NOT_PUBLISHED`) **فقط إذا كانت المحاولة الجديدة تستخدم authorization_id/job_id مختلفين** عن المحاولة التي فشلت. نحن تعمّدنا **إعادة استخدام نفس الـjob ونفس التفويض** (تنفيذًا لتعليمات "لا تُنشئ سجلات مكررة") — وهذا بالتحديد ما جعل النظام يعتبر إعادة المحاولة "غير مؤهَّلة"، لأن الحماية مصمَّمة لمنع إعادة استخدام نفس زوج (authorization_id, job_id) حرفيًا.

هذا تعارض حقيقي بين قاعدتين من قواعد الأمان الموجودة أصلاً (منع التكرار في السجلات، مقابل منع إعادة استخدام نفس authorization/job لإعادة محاولة) — **لم يُصلَح بعد** (هذه الجولة تشخيص فقط دون أي تعديل، بناءً على طلب صريح). الحالة النهائية: job=`dead`، الإيصال=`failed` (دون تغيير)، لا نشر، لا تكرار، لا لمس لـInstagram.

## 10.6 محدَّث 2026-08-16 (تأكيد لقطة الشاشة قديمة + إصلاح شرط إعادة تفعيل الإيصال + محاولة أخيرة جاهزة)

راجع المالك لقطة الشاشة بعناية: الطابع الزمني الظاهر **6 أغسطس، 1:57 صباحًا** — منشور قديم حقيقي، **وليس** نتيجة execution 1060. تأكَّد رسميًا: **execution 1060 لم ينشر على Facebook إطلاقًا**، ولا حاجة لمزيد من فحص لقطات الشاشة.

**السبب الجذري الدقيق لعدم قابلية إعادة المحاولة**: دالة `reserve_authorized_facebook_publication_receipt` كانت ترفض إعادة تفعيل إيصال بحالة `failed`/`MANUAL_CONFIRMED_NOT_PUBLISHED` إذا كانت محاولة إعادة الحجز تستخدم **نفس** (authorization_id, job_id) للمحاولة الفاشلة. لكن قيد `UNIQUE` على `owner_publish_authorizations.publish_job_id` يجعل إنشاء تفويض جديد لنفس الـjob **مستحيلًا هيكليًا** — وبما أن التعليمات تمنع إنشاء job جديد، كان هذا الشرط يجعل مسار الاستعادة **غير قابل للتحقيق إطلاقًا** لأي إعادة محاولة تحترم قاعدة "لا سجلات مكررة".

**الإصلاح المطبَّق (الحد الأدنى الآمن)**: حذف الشرط الثالث `(authorization_id=p_authorization_id and publish_job_id=p_job_id)` من شرط الرفض فقط. البوابة الحقيقية للأمان تبقى كما هي تمامًا: لا يمكن إعادة تفعيل أي إيصال إلا إذا كانت حالته `failed` وسببه بالتحديد `MANUAL_CONFIRMED_NOT_PUBLISHED:%` — وهي حالة لا تتحقق إلا بعد تأكيد يدوي موثَّق من المالك عبر `confirm_ambiguous_publish_not_sent`. لا تغيير على حالات `reserved`/`container_created`/`published`/`ambiguous` — الحماية من التكرار والنشر المزدوج سليمة تمامًا كما كانت.

**إعادة التسليح**: نفس الـjob (`3e6b396c`) ونفس التفويض (`64846a26`) فقط — لا عناصر أو سجلات جديدة.

**النتيجة**: تشغيل واحد (execution **1066**) وصل بنجاح إلى Owner Approval وهو **منتظر الآن** (لا مهلة زمنية)، مع إصلاح توجيه Photo/Text (من التحديث 10.4) لا يزال ساريًا.

**رابط الموافقة الحي**: `https://relaxfixuae.app.n8n.cloud/form-waiting/1066?signature=975bcada217c573be653aff9009141c87cdf1547d6fa8d053805687eb105b5e2`

## 11. تذكير إلزامي لكل Agent

- لا تبدأ من الصفر — التزم بـCURRENT_PHASE أعلاه.
- لا تعِد فحص البنود المغلقة في القسم 5.
- لا تستخدم `test-content-0001` كدليل جاهزية أو للنشر.
- أي نشر فعلي لعنصر Facebook يتطلب موافقة صريحة من المالك بعد عرض العنصر عليه، ضمن التفويض المحدود المذكور في القسم 9 (منشور واحد فقط، بدون Boost أو إعلانات أو إعادة نشر).
- عند غموض نتيجة النشر: لا تُعِد المحاولة تلقائيًا؛ أبلغ المدير/المالك.
- التزم بحدود الأنظمة المعتمدة في القسم 12 (APPROVED SYSTEM BOUNDARIES) ولا تلمس الأنظمة أو الحسابات غير المدرجة فيه.

## 12. APPROVED SYSTEM BOUNDARIES

- Active Supabase project only: `nmzxrjdxvmmzzmajrskm`
- Never touch inactive Supabase: `aazhniddjvhuimlxxjfd`
- Approved n8n workflow only: `xNwYPSXQiUyzDSyZ`
- Old workflows must remain inactive and untouched: `7OVKtZ2TAZsrDIXc`, `Vj8Xh4UQ534LYist`
- Facebook Page ID: `1164107840123575`
- Instagram Account ID: `17841439747493221`
- Never expose tokens or secrets in documentation.

## 13. FUTURE PHASE ASSETS — DO NOT REBUILD

نتيجة Second-Pass Discovery Audit (2026-08-03) على مستودع الموقع العام `swim-fluent-uae` وSupabase الفعّال وn8n المعتمد. لا تُعاد بناء ما يلي عند وصول دور المراحل 6-8؛ التحقق النهائي من الحسابات الخارجية يبقى مطلوبًا أولًا:

- **SEO**: البنية التقنية (sitemap.xml, robots.txt, canonical, Open Graph, Schema.org JSON-LD لـ Organization/Person/Service) مبنية ومختبرة بعقود في `swim-fluent-uae`؛ موثّقة بتفصيل في `docs/seo/`.
- **GA4**: الكود موجود (Consent Mode v2) لكنه معطّل (`VITE_ENABLE_GA4=false`) وبدون Measurement ID مؤكد.
- **Chatbot الموقع**: واجهة FAQ أمامية مبنية ومتحقَّق منها بصريًا مرة واحدة في Preview، لكنها معطّلة بـ Feature Flag ولا تُخزّن أو ترسل أي بيانات.
- **Supabase**: جداول `conversations`/`leads`/`knowledge_entries` جاهزة هيكليًا للمرحلة 8 لكنها فارغة (0 صف).
- لا يوجد اتصال حي مؤكد بـ WhatsApp Business API أو Facebook Messenger أو Instagram Messaging (لا Webhooks، لا بيانات مسجَّلة).
- **Vercel Production / الدومين / Google Search Console / Google Business Profile**: BLOCKED_BY_ACCESS أو UNVERIFIED — لم تُفحص مباشرة في هذا الجرد؛ تتطلب دخول المالك عند وصول دور المرحلة المعنية فقط.
